% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_battery.R
\name{pivot_battery}
\alias{pivot_battery}
\title{Reshape \code{calc_stats()} output into a battery/grid table (items x category
levels)}
\usage{
pivot_battery(data)
}
\arguments{
\item{data}{A tibble as returned by \code{\link[=calc_stats]{calc_stats()}}, called with a
consistent category-level outcome set and a single statistic.}
}
\value{
A list: \code{pivoted}, \code{stat_code}, \code{category_order}, and
\code{conf_type} ("se", "ci", or \code{NA}). Pass this list straight to
\code{\link[=format_battery]{format_battery()}}.
}
\description{
For a set of outcome variables sharing consistent category levels (e.g.
a Likert battery, Strongly agree...Strongly disagree) - one row per
item, one column per category level, with a common item-label stem
detected and stripped automatically (via \code{find_common_stem()}) where
possible. Only one statistic per call (e.g. \code{"perc"}); \code{Total} cross-break
only, like \code{\link[=pivot_numeric_summary]{pivot_numeric_summary()}}.
}
\seealso{
\code{\link[=format_battery]{format_battery()}}, \code{\link[=make_battery_table]{make_battery_table()}}, \code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
