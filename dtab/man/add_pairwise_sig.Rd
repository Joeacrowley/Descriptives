% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_stats.R
\name{add_pairwise_sig}
\alias{add_pairwise_sig}
\title{Add letter-based pairwise significance testing to \code{calc_stats()} output}
\usage{
add_pairwise_sig(data, alpha = 0.05)
}
\arguments{
\item{data}{A tibble as returned by \code{\link[=calc_stats]{calc_stats()}}, with at least one
predictor (a \code{cross_break} column with more than just \code{"Total"}).}

\item{alpha}{Significance threshold. Default 0.05.}
}
\value{
\code{data}, with two columns added: \code{sig_letter} (this row's own
assigned letter) and \code{sig_diff} (the letters of levels it's
significantly different from). Both \code{NA} for rows whose \code{stat} isn't
pairwise-eligible.
}
\description{
Compares every pair of predictor levels within each (outcome, stat,
o_cat) group and assigns significance letters (columns sharing a
letter are not significantly different at \code{alpha}) plus a \code{sig_diff}
column listing which other levels a given level IS significantly
different from. Only rows whose \code{stat} is in \code{pairwise_eligible_stats}
(\code{perc}, \code{w_perc}, \code{count}, \code{w_count}, \code{mean}, \code{w_mean}, \code{median},
\code{w_median}) are tested; an exact covariance-aware test is used where
one is available (\code{w_perc}, \code{w_count}, \code{w_mean}, via \code{svyby()}/
\code{svycontrast()}), and an approximate independent-SE test (zero
cross-group covariance, but each group's own real design-based SE)
everywhere else. Fully usable standalone on any \code{calc_stats()}-shaped
tibble - \code{calc_stats(pairwise = TRUE)} calling this internally is just
one caller among others.
}
\seealso{
\code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
