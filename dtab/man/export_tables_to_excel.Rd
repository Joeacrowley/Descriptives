% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_to_excel.R
\name{export_tables_to_excel}
\alias{export_tables_to_excel}
\title{Export any number of huxtables to one .xlsx file, one sheet per table}
\usage{
export_tables_to_excel(
  hux_list,
  file,
  sheet_names = NULL,
  titles = NULL,
  stem_cols = NULL,
  max_stem_width = 45
)
}
\arguments{
\item{hux_list}{A list of \code{huxtable} objects, as returned by any of this
package's \verb{format_*()} functions (or \verb{make_*()} with
\code{formatted = TRUE}).}

\item{file}{Output path for the .xlsx file.}

\item{sheet_names}{Optional character vector of sheet names, one per
table. Defaults to \code{"Table 1"}, \code{"Table 2"}, ...}

\item{titles}{Optional character vector of title-row text, one per
table (also used for the Contents sheet's listing, if shown).}

\item{stem_cols}{Optional column width override(s) for the stub
column(s), passed through to the underlying worksheet writer.}

\item{max_stem_width}{Maximum stub column width, in Excel column-width
units. Default 45.}
}
\value{
Invisibly, the \code{openxlsx} workbook object. Primarily called for
its side effect of writing \code{file}.
}
\description{
Writes each table to its own sheet, and - for 2 or more tables only -
adds an auto-generated, hyperlinked Contents sheet placed first. For a
single table, use \code{\link[=export_table_to_excel]{export_table_to_excel()}} instead (a thin wrapper
around this function that skips the Contents sheet entirely).
}
\seealso{
\code{\link[=export_table_to_excel]{export_table_to_excel()}}
}
