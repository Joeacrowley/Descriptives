% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_nested_crosstab.R
\name{pivot_nested_crosstab}
\alias{pivot_nested_crosstab}
\title{Reshape \code{calc_stats()} output into a nested crosstab (two-variable columns)}
\usage{
pivot_nested_crosstab(data)
}
\arguments{
\item{data}{A tibble as returned by \code{\link[=calc_stats]{calc_stats()}}, called with a nested
\code{predictors} set (\verb{predictors = list(c(<outer variable>, <inner variable>))}).}
}
\value{
A list: \code{pivoted}, \code{bases}, \code{col_order}, \code{header_info},
\code{conf_type} ("se", "ci", or \code{NA}), and \code{legend} (a tibble, or \code{NULL}
if \code{pairwise = TRUE} wasn't used). Pass this list straight to
\code{\link[=format_nested_crosstab]{format_nested_crosstab()}}.
}
\description{
For a single NESTED predictor set (\verb{predictors = list(c(<outer>, <inner>))}) - a 4-row header with the outer variable's levels
(e.g. Sex) each split into the inner variable's levels (e.g. AgeGroup)
beneath it, one p-value column per outer level ("among women, is age
associated with this outcome?"). For a flat (single-variable, or several
single-variable) predictor set instead, use \code{\link[=pivot_crosstab]{pivot_crosstab()}}.
}
\seealso{
\code{\link[=format_nested_crosstab]{format_nested_crosstab()}}, \code{\link[=pivot_crosstab]{pivot_crosstab()}}, \code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
