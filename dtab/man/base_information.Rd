% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_stats.R
\name{base_information}
\alias{base_information}
\title{Package general and per-variable base descriptions for \code{calc_stats()}'s
\code{base} argument}
\usage{
base_information(data, general_base, specific_bases)
}
\arguments{
\item{data}{A data frame, or survey design object.}

\item{general_base}{Character string describing the base that applies
to any outcome without its own entry in \code{specific_bases}.}

\item{specific_bases}{A named list, names are outcome variable names,
values are that outcome's own base description (overrides
\code{general_base} for that variable).}
}
\value{
A list of 4 elements consumed internally by \code{calc_stats()} and
its footnote-building helpers: \code{general_base}, \code{specific_bases}, the
variable labels for variables in \code{specific_bases}, and the variable
labels for every variable in \code{data}.
}
\description{
Bundles a general (fallback) base description with a named list of
variable-specific base descriptions, plus the variable labels needed to
render each one as readable footnote text. Feed the result to
\code{calc_stats()}'s \code{base} argument; \code{create_bases()}/
\code{prepare_base_for_table()} (internal) turn it into the actual footnote
text each \verb{format_*()} function attaches to its table.
}
\seealso{
\code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
