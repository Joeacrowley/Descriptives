% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_numeric_summary.R
\name{pivot_numeric_summary}
\alias{pivot_numeric_summary}
\title{Build a numeric-only summary table (variables x statistics), calling
\code{calc_stats()} internally}
\usage{
pivot_numeric_summary(
  data,
  outcomes,
  statistics = c("mean", "sd"),
  weighted = FALSE,
  base = NULL
)
}
\arguments{
\item{data}{A data frame, or survey design object if \code{weighted = TRUE}.}

\item{outcomes}{Character vector of numeric variable names to summarise.}

\item{statistics}{One or more of \code{mean}, \code{median}, \code{sum}, \code{min}, \code{max},
\code{range}, \code{iqr}, \code{sd}. Default \code{c("mean", "sd")}.}

\item{weighted}{If \code{TRUE}, requests the weighted equivalent of each
statistic that has one (\code{data} must then be a survey design object).
\code{min}/\code{max}/\code{range} have no weighted equivalent and are returned
unweighted regardless.}

\item{base}{Optional base/filter expression, passed through to
\code{\link[=calc_stats]{calc_stats()}}.}
}
\value{
A list: \code{pivoted} (one row per variable, one column per
statistic, plus \code{Base}) and \code{stat_lookup} (a named vector mapping each
display column name back to its underlying stat code, for
\code{\link[=format_numeric_summary]{format_numeric_summary()}}'s digit rules). Pass this list straight to
\code{\link[=format_numeric_summary]{format_numeric_summary()}}.
}
\description{
Rows are variables, columns are statistics (Mean, SD, Min, ...), one
number per cell - no predictor/crossbreak dimension, always a single
overall ("Total") summary. Unlike \code{\link[=pivot_summary]{pivot_summary()}}/\code{\link[=pivot_crosstab]{pivot_crosstab()}},
this function calls \code{\link[=calc_stats]{calc_stats()}} itself rather than taking its output,
because \code{weighted} has to choose the exact stat codes (\code{mean} vs
\code{w_mean}, ...) before \code{calc_stats()} ever runs.
}
\seealso{
\code{\link[=format_numeric_summary]{format_numeric_summary()}}, \code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
