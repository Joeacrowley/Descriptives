% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_battery_table.R
\name{make_battery_table}
\alias{make_battery_table}
\title{Build a battery/grid table in one call}
\usage{
make_battery_table(
  data,
  outcomes,
  statistics = "perc",
  conf = NULL,
  base = NULL,
  multicode = FALSE,
  formatted = TRUE
)
}
\arguments{
\item{data}{A data frame, or survey design object.}

\item{outcomes}{Character vector of categorical/multicoded outcome
variable names sharing consistent category levels (e.g. a Likert
battery).}

\item{statistics}{Exactly one statistic code: \code{"perc"}, \code{"count"},
\code{"w_perc"}, or \code{"w_count"}.}

\item{conf}{\code{NULL}, \code{"se"}, or \code{"ci"} - inserted as an extra row under
each item when requested.}

\item{base}{Optional base/filter expression, passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{multicode}{Passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{formatted}{If \code{TRUE} (default), returns the styled \code{huxtable}.
If \code{FALSE}, returns \code{\link[=pivot_battery]{pivot_battery()}}'s own list output untouched
(\code{pivoted}, \code{stat_code}, \code{category_order}, \code{conf_type}).}
}
\value{
A styled \code{huxtable}, or (if \code{formatted = FALSE}) the list
returned by \code{\link[=pivot_battery]{pivot_battery()}}.
}
\description{
Single entry point chaining \code{\link[=calc_stats]{calc_stats()}} straight to \code{\link[=pivot_battery]{pivot_battery()}}
and \code{\link[=format_battery]{format_battery()}} - the battery-grid equivalent of \code{\link[=make_table]{make_table()}}'s
one-call convenience, kept as a separate function rather than folded
into \code{make_table()} for the same dispatch-collision reason as
\code{\link[=make_numeric_summary_table]{make_numeric_summary_table()}} (see this file's header note:
\code{predictors = NULL} already routes \code{make_table()} to a plain summary
table, and a battery grid is also a no-predictors table).
}
\seealso{
\code{\link[=pivot_battery]{pivot_battery()}}, \code{\link[=format_battery]{format_battery()}}
}
