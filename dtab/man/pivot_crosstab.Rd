% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_crosstab.R
\name{pivot_crosstab}
\alias{pivot_crosstab}
\title{Reshape \code{calc_stats()} output into a crosstab, predictor levels as columns}
\usage{
pivot_crosstab(data)
}
\arguments{
\item{data}{A tibble as returned by \code{\link[=calc_stats]{calc_stats()}}, with at least one
flat predictor (\code{predictors} not \code{NULL}, and no element of length > 1).}
}
\value{
A list: \code{pivoted}, \code{bases}, \code{all_level_cols}, \code{predictor_sets}
(a named vector - \code{cross_break} as names, display label as values),
\code{conf_type} ("se", "ci", or \code{NA}), and \code{legend} (a tibble, or \code{NULL}
if \code{pairwise = TRUE} wasn't used). Pass this list straight to
\code{\link[=format_crosstab]{format_crosstab()}}.
}
\description{
One or more FLAT predictor sets (each \code{predictors} element of length 1),
shown side by side if more than one was requested - a Sex block next to
an AgeGroup block, for example. For a single 2-variable nested predictor
set instead, use \code{\link[=pivot_nested_crosstab]{pivot_nested_crosstab()}}, not this function.
}
\details{
Supports \code{calc_stats(pairwise = TRUE)} output: a \code{legend} (returned as
the 6th list element) and \code{"<level>__sigdiff"} columns feed
\code{\link[=format_crosstab]{format_crosstab()}}'s inserted legend row and Sig. diff row. A
categorical variable is limited to exactly one statistic per call;
a numeric variable can have more than one (e.g. mean + median).
}
\seealso{
\code{\link[=format_crosstab]{format_crosstab()}}, \code{\link[=pivot_nested_crosstab]{pivot_nested_crosstab()}}, \code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
