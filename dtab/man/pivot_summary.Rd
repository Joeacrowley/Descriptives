% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_summary.R
\name{pivot_summary}
\alias{pivot_summary}
\title{Reshape \code{calc_stats()} output into a Total-only summary table}
\usage{
pivot_summary(data)
}
\arguments{
\item{data}{A tibble as returned by \code{\link[=calc_stats]{calc_stats()}}.}
}
\value{
A list: \code{pivoted} (the reshaped tibble, with \code{row_type},
\code{stat_code}, \code{block_id}, and \code{is_variable_row} metadata columns) and
\code{bases} (footnote text from \code{base_description}, via
\code{prepare_base_for_table()}). Pass this list straight to
\code{\link[=format_summary]{format_summary()}}.
}
\description{
Collapses Variable and Levels into a single "Variable" column: a numeric
variable's label sits directly on its one data row, while a categorical
variable gets a label row above its levels. Meant for a \code{calc_stats()}
call with no predictor (or, if a predictor was supplied, only its
"Total" rows are used - see \code{\link[=pivot_crosstab]{pivot_crosstab()}} for a real crosstab).
}
\seealso{
\code{\link[=format_summary]{format_summary()}}, \code{\link[=calc_stats]{calc_stats()}}
}
\keyword{internal}
