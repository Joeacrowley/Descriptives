% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_numeric_summary_table.R
\name{make_numeric_summary_table}
\alias{make_numeric_summary_table}
\title{Build a numeric-only summary table in one call}
\usage{
make_numeric_summary_table(
  data,
  outcomes,
  statistics = c("mean", "sd"),
  weighted = FALSE,
  base = NULL,
  formatted = TRUE
)
}
\arguments{
\item{data}{A data frame, or survey design object if \code{weighted = TRUE}.}

\item{outcomes}{Character vector of numeric variable names to summarise.}

\item{statistics}{One or more of \code{mean}, \code{median}, \code{sum}, \code{min}, \code{max},
\code{range}, \code{iqr}, \code{sd}. Default \code{c("mean", "sd")}.}

\item{weighted}{If \code{TRUE}, requests the weighted equivalent of each
statistic that has one (\code{data} must then be a survey design object).}

\item{base}{Optional base/filter expression, passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{formatted}{If \code{TRUE} (default), returns the styled \code{huxtable}.
If \code{FALSE}, returns \code{\link[=pivot_numeric_summary]{pivot_numeric_summary()}}'s own list output
untouched.}
}
\value{
A styled \code{huxtable}, or (if \code{formatted = FALSE}) the list
returned by \code{\link[=pivot_numeric_summary]{pivot_numeric_summary()}}.
}
\description{
Single entry point chaining \code{\link[=pivot_numeric_summary]{pivot_numeric_summary()}} straight to
\code{\link[=format_numeric_summary]{format_numeric_summary()}} - the numeric-summary equivalent of
\code{\link[=make_table]{make_table()}}'s one-call convenience, kept as a separate function
rather than folded into \code{make_table()} (see this file's header note for
the dispatch-collision reasoning: \code{predictors = NULL} already routes
\code{make_table()} to a plain summary table, and a numeric-statistics grid
is also a no-predictors table).
}
\seealso{
\code{\link[=pivot_numeric_summary]{pivot_numeric_summary()}}, \code{\link[=format_numeric_summary]{format_numeric_summary()}}
}
