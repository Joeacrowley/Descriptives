% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_table.R
\name{make_table}
\alias{make_table}
\title{Single entry point: \code{calc_stats()} chained straight to the right
pivot/format pair}
\usage{
make_table(
  data,
  outcomes,
  predictors = NULL,
  statistics = c("count", "mean"),
  conf = NULL,
  base = NULL,
  pval = NULL,
  multicode = TRUE,
  pairwise = FALSE,
  formatted = TRUE
)
}
\arguments{
\item{data}{A data frame, or survey design object.}

\item{outcomes}{Character vector of outcome variable names.}

\item{predictors}{\code{NULL} for a Total-only summary, a single variable
name or list of variable names for a flat crosstab (one or more
side-by-side predictor blocks), or \verb{list(c(<outer>, <inner>))} for a
single nested crosstab. Mixing a nested set with additional flat sets
in one call is rejected - run two separate \code{make_table()} calls
instead.}

\item{statistics}{Character vector of statistic codes, passed to
\code{\link[=calc_stats]{calc_stats()}}.}

\item{conf}{\code{NULL}, \code{"se"}, or \code{"ci"}, passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{base}{Optional base/filter expression, passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{pval}{Optional p-value method, passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{multicode}{Passed to \code{\link[=calc_stats]{calc_stats()}}.}

\item{pairwise}{If \code{TRUE}, passed to \code{\link[=calc_stats]{calc_stats()}} - runs letter-based
pairwise significance testing across predictor levels and adds the
legend block/Sig. diff row wherever there's a predictor to compare
across (\code{\link[=pivot_crosstab]{pivot_crosstab()}}/\code{\link[=pivot_nested_crosstab]{pivot_nested_crosstab()}} and their
\verb{format_*()} partners already render this automatically). A no-op for
a Total-only table (\code{predictors = NULL}), since there's nothing to
compare.}

\item{formatted}{If \code{TRUE} (default), returns the styled \code{huxtable}.
If \code{FALSE}, stops after the pivot step and returns the matching
\verb{pivot_*()} function's own list output untouched (no digit formatting
or presentation-only rows), for further computation.}
}
\value{
A styled \code{huxtable}, or (if \code{formatted = FALSE}) the list
returned by whichever \verb{pivot_*()} function was dispatched to.
}
\description{
Dispatches on the shape of \code{predictors} to decide which table this is -
a Total-only summary (\code{\link[=pivot_summary]{pivot_summary()}}), a flat crosstab
(\code{\link[=pivot_crosstab]{pivot_crosstab()}}), or a single nested crosstab
(\code{\link[=pivot_nested_crosstab]{pivot_nested_crosstab()}}) - so a normal call site never has to name
any of those three functions (or their \verb{format_*()} partners) directly.
}
\seealso{
\code{\link[=calc_stats]{calc_stats()}}, \code{\link[=pivot_summary]{pivot_summary()}}, \code{\link[=pivot_crosstab]{pivot_crosstab()}},
\code{\link[=pivot_nested_crosstab]{pivot_nested_crosstab()}}
}
